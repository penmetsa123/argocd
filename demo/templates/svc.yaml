apiVersion: v1
kind: Service
metadata:
  name: mithun-service
  namespace: stg
spec:
  selector:
    name: mithun
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 32000
